require "spec_helper"
require "recording_strategies/file_frames_strategy"

module TRecs
  describe FileFramesStrategy do
    context "initialization" do
       skip("FileFramesStrategy") 
    end
  end
end
